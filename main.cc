#include "algoritmos.hh"
#include "grafoLista.hh"
//#include "grafoMatriz.hh"

int main() {
/*
    Grafo grafo;
    grafo.AgregarVert(13);
    grafo.AgregarVert(23);
    grafo.AgregarVert(33);
    grafo.AgregarArist(grafo.EtiqAVert(13), grafo.EtiqAVert(23), 0.6);
    grafo.AgregarArist(grafo.EtiqAVert(33), grafo.EtiqAVert(23), 0.7);
    //grafo.BorrarArist(grafo.EtiqAVert(33), grafo.EtiqAVert(23));
    grafo.Imprimir();
    Algoritmos alg;
    std::cout << std::endl;
    std::cout << std::endl;
    std::cout << alg.NumAristas(&grafo) << std::endl;
    std::cout << alg.EsConexoAnchoPrim(&grafo) << std::endl;
*/
    return 0;
}
